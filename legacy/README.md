# Legacy Files

This folder keeps earlier experiment scripts and source snapshots for reference only.

The current app is:

```text
CodexFloat.cs
CodexFloat.exe
```

Legacy files may still contain old internal names such as `CodexResetMonitor`; they are not part of the current build.
